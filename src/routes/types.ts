interface Routes {
  login: string;
  myExercises: string;
  create: string;
}

const endpoints: Routes = {
  login: "/login",
  myExercises: "/my-exercises",
  create: "/create",
};

export default endpoints;
